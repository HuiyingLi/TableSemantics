#This file receives test samples represented in feature vector
#and predict the label (header or field) for them. The SVM model
#is trained on 500 headers and 453 human annotated fields. 
from scikit import svm

